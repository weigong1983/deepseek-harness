# 代码更新日志

> 记录针对本开源项目（`deepseek-ai/deepseek-harness`）的所有代码级修改。
> 本日志由人工/助手在运行调试过程中维护，便于后续回滚、同步官方更新或提交 PR。

---

## 2026-09-12 修改点 1：放宽 loopback 场景下的 Origin/Host 端口校验

### 问题背景

在 macOS 环境下运行 `dsh web` 后，浏览器访问 `http://127.0.0.1:3080` 时，所有 `/api/*` 请求返回 **HTTP 403**，例如：

```
transport failure for /api/directoryPicker/pick: HTTP 403
transport failure for /api/settings/describe: HTTP 403
transport failure for /api/session/list: HTTP 403
...
```

### 根因分析

根因位于 `/api` 的 trust fence：`packages/client/connection/src/api-request-trust.ts` 中的 `isTrustedApiRequest`。

校验逻辑要求：

```ts
new URL(origin).host === hostUrl.host
```

即 `Origin` 与 `Host` 必须完全一致（含端口）。

但在某些网络环境（如 Cisco AnyConnect VPN、公司代理或系统代理）下，浏览器实际请求中的 `Host` 头会被改写为 `127.0.0.1`（不带 `:3080`），而浏览器自动附带的 `Origin: http://127.0.0.1:3080` 仍带端口。两者端口不一致，触发 403。

通过 curl 复现：

- `Host: 127.0.0.1:3080` + `Origin: http://127.0.0.1:3080` → **401**（trust fence 通过，仅缺 cookie）
- `Host: 127.0.0.1` + `Origin: http://127.0.0.1:3080` → **403**（端口不一致，被 trust fence 拒绝）

### 修改内容

**源码位置**：`packages/client/connection/src/api-request-trust.ts:111-118`

将原来的严格 host 比较，改为 **loopback 场景下只比较 hostname，非 loopback 仍严格比较 host（含端口）**。

```diff
--- a/packages/client/connection/src/api-request-trust.ts
+++ b/packages/client/connection/src/api-request-trust.ts
@@ -111,7 +111,12 @@ export function isTrustedApiRequest(request: ConnectionTrustRequest, trustedHost
   const origin = header(request.headers, 'origin')
   if (origin === undefined) return true
   try {
-    return new URL(origin).host === hostUrl.host
+    const originUrl = new URL(origin)
+    if (isLoopbackHostname(hostUrl.hostname) && isLoopbackHostname(originUrl.hostname)) {
+      // Loopback is safe even if a proxy rewrote the Host port away.
+      return originUrl.hostname === hostUrl.hostname
+    }
+    return originUrl.host === hostUrl.host
   } catch {
     return false
   }
```

### 实际应用位置

为快速验证，本次直接 patch 了 npm 全局安装包中的已编译产物：

- 文件：`/Users/teku/.npm-global/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/dsh-client-connection/lib/index.js`
- 行号：约第 208-214 行
- 修改方式：与上述源码 patch 逻辑一致

### 安全说明

- 此修改仅在 **Host 和 Origin 都是 loopback（127.0.0.1 / localhost / ::1）** 时放宽端口检查。
- 非 loopback 场景仍保持原严格校验，不影响 DNS rebinding / 跨站防御能力。
- 这是一个临时/本地修复，官方修复后应移除。

### 后续 TODO

- [ ] 从源码重新 build，将 patch 落到源码版本。
- [ ] 关注官方仓库是否已修复该问题，若有则回滚本地 patch 并升级。
- [ ] 考虑是否需要向 deepseek-harness 仓库提交 issue/PR。

---
